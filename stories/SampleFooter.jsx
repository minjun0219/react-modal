import React from 'react';

const styles = {
  display: 'block',
  width: '100%',
  borderTop: '1px solid rgba(0, 0, 0, .1)',
  lineHeight: '48px',
  textAlign: 'center',
  background: '#fff',
};

const SampleFooter = () => (
  <div style={styles}>
    Footer
  </div>
);

export default SampleFooter;
